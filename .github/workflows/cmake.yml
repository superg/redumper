name: CMake

on:
  push:
    branches: [ "main" ]
    tags-ignore:
      - '**'
#  pull_request:
#    branches: [ "main" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  build:
    strategy:
      matrix:
        platform: [Win32]
        
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: GitHub Release
      run: hub release create -m "test title" "test date"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


#    - name: Configure
#      run: cmake -B ${{github.workspace}}/build.${{matrix.platform}} -G "Visual Studio 17 2022" -A ${{matrix.platform}} -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DREDUMPER_VERSION_PATCH=${{github.run_number}}
#
#    - name: Build
#      run: cmake --build ${{github.workspace}}/build.${{matrix.platform}} --config ${{env.BUILD_TYPE}}
#      
#    - name: Test
#      working-directory: ${{github.workspace}}/build.${{matrix.platform}}
#      run: ctest -C ${{env.BUILD_TYPE}}
#
#    - name: Package
#      working-directory: ${{github.workspace}}/build.${{matrix.platform}}
#      run: cpack -G ZIP --config CPackConfig.cmake
#
#    - name: Upload
#      uses: actions/upload-artifact@v3
#      with:
#        path: ${{github.workspace}}/build.${{matrix.platform}}/redumper-*.zip
#    - name: Release
#      uses: softprops/action-gh-release@v1
#      with:
#        tag_name: BUILD
#        files: ${{github.workspace}}/build.${{matrix.platform}}/redumper-*.zip